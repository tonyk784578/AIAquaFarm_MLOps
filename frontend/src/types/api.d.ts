/**
 * Auto-generated OpenAPI types — DO NOT EDIT.
 *
 * Regenerate with:
 *
 *   make codegen                # while `make up` is running
 *   # or, from frontend/:
 *   npm run codegen             # fetches from $BACKEND_OPENAPI_URL or localhost:8000
 *   npm run codegen:file        # reads ../backend/openapi.json
 *
 * The hand-written types in `./index.ts` are still authoritative for the
 * dashboard. As endpoints stabilise, migrate consumers to types exported
 * from this file:
 *
 *   import type { components } from '@/types/api'
 *   type WaterQualityReading = components['schemas']['WaterQualityReading']
 *
 * Until the first codegen run, this stub keeps the module import resolvable.
 */

export type paths = Record<string, never>
export type components = {
  schemas: Record<string, never>
}
