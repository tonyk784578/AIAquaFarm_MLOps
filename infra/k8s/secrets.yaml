# Secret template — do NOT commit real values.
# In production, replace stringData values via Sealed Secrets,
# External Secrets Operator, or your CI/CD vault injection step.
apiVersion: v1
kind: Secret
metadata:
  name: aquafarm-secrets
  namespace: aquafarm
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE_ME"
  SECRET_KEY: "CHANGE_ME_32_CHAR_MIN_USE_openssl_rand_hex_32"
  # Separate secret for refresh tokens — use: openssl rand -hex 32
  JWT_REFRESH_SECRET_KEY: "CHANGE_ME_DIFFERENT_FROM_SECRET_KEY"
  # Shared key for agent→backend internal calls — use: openssl rand -hex 32
  INTERNAL_API_KEY: "CHANGE_ME_INTERNAL_KEY"
  BACKEND_API_KEY: "CHANGE_ME_INTERNAL_KEY"
  ANTHROPIC_API_KEY: "sk-ant-CHANGE_ME"
  S3_ACCESS_KEY: "CHANGE_ME"
  S3_SECRET_KEY: "CHANGE_ME"
  S3_ENDPOINT_URL: "http://minio:9000"
  S3_BUCKET_NAME: "aquafarm-datalake"
